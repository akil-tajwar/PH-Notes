var numbers = [54, 23, 64, 74];

// showing output of array
console.log(numbers);

// showing the length of a particular array
console.log(numbers.length);

// showing an element of array by the index number
console.log(numbers[2]);

// showing the position of an element of array by this method
console.log(numbers.indexOf(54));